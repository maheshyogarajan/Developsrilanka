"""fiesta.business — B12 Business Income UI surface (MS3 Stage E.1).

Routes are registered via ``fiesta.business.routes.register_blueprint(app)``.
The pure compute / persistence layer lives in ``fiesta.tax.business_income``.
"""
