"""fiesta.employment — G3.1 Employment Income (LKR + APIT) UI surface.

Routes are registered via ``fiesta.employment.routes.register_blueprint(app)``.
The pure compute / persistence layer lives in ``fiesta.tax.employment``.
"""
