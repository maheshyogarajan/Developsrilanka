"""fiesta.rsu — B11 RSU UI surface (MS2 Stage E.1).

Routes are registered via ``fiesta.rsu.routes.register_blueprint(app)``.
The pure compute / persistence layer lives in ``fiesta.tax.rsu_engine``.
"""
