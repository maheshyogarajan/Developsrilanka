"""fiesta.investment_local — G3.5 LOCAL Investment Income UI surface (MS4 W3c).

Routes are registered via
``fiesta.investment_local.routes.register_blueprint(app)``.
The pure compute / persistence layer lives in
``fiesta.tax.investment_local``.
"""
