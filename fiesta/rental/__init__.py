"""fiesta.rental — G3.4 LKR Rental Income UI surface (MS4 W3c).

Routes are registered via ``fiesta.rental.routes.register_blueprint(app)``.
The pure compute / persistence layer lives in ``fiesta.tax.rental_lkr``.
"""
